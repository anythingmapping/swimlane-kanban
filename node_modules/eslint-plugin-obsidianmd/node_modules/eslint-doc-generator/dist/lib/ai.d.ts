import { AI_PROVIDER } from './types.js';
type REQUEST_PROTOCOL = 'openaiCompatible' | 'anthropic';
export declare const SUPPORTED_API_KEY_ENV_VARS: string[];
export interface AiRequestOptions {
    aiProvider: AI_PROVIDER | undefined;
    aiModel: string | undefined;
}
export interface AiProviderConfig {
    provider: AI_PROVIDER;
    apiKey: string;
    model: string;
    endpoint: string;
    protocol: REQUEST_PROTOCOL;
}
export interface AiJsonRequestPrompt {
    readonly systemPrompt?: string;
    readonly userPrompt: string;
}
export declare function resolveAiProviderConfig({ aiProvider, aiModel, }: AiRequestOptions): AiProviderConfig;
export declare function requestAiJsonObject(providerConfig: AiProviderConfig, prompt: AiJsonRequestPrompt): Promise<Record<string, unknown>>;
export {};
